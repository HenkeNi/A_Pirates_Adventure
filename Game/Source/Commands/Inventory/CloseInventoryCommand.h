#pragma	 once
#include "../Base/Command.h"

// Renaem MenuCommand? InGameMenuCommand? Scenes should contain a menu folder?
// Combine with OpenInventoryCommand? Or put in same file?
class CloseInventoryCommand : public Command					// todo, have an undo command instead of a close command?
{
public:
};